import { useState, useContext } from "react"
import { AlbumContext } from "@/context/AlbumContext"


const AlbumView = () => {

    // if album(from context).id === [id] the use that data set? 
    
    return (
        <>
            <h1>Hello World</h1>
        </>
    )

}

export default AlbumView